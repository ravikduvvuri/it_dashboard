# Labeled Data

Place labeled training data in this folder.

Expected format:
- CSV file
- columns: description, clean_text, category

You can generate a labeled dataset by preprocessing raw data with `src/preprocess.py`, then adding a `category` label column.
